\documentclass[tikz, convert={convertexe={magick}}]{standalone} % Use in previewing

\standaloneconfig{border=5pt} 

\usepackage{lmodern}
\usepackage[T1]{fontenc}
%\usepackage[useregional]{datetime2}

% Para usar com PDFTEX
\usepackage[brazilian]{babel}
\usepackage[utf8]{inputenc}
\usepackage{amsmath,xfrac}

% Para usar com XELATEX
%\usepackage{polyglossia}
%\setdefaultlanguage{brazil}

% preview
%\usepackage[active,tightpage]{preview}
%\PreviewEnvironment{tikzpicture} % uncomment for previewing tikzpicture
%%\PreviewEnvironment{circuitikz} % uncomment for previewing circuitikz
%\setlength\PreviewBorder{5pt}

\usepackage{pgfplots}
\pgfplotsset{compat=1.5,
tick label style={font=\normalsize},
label style={font=\normalsize},
legend style={font=\normalsize},}

\begin{document}
\centering
\begin{tikzpicture}
	\begin{axis}[
			%	title={Contínua},
			axis lines=left,
			ylabel={$ V $},
			xlabel={$ t $},
			xtick=\empty,
			ytick=\empty,
			xmin=0,xmax=5,
			ymin=0,ymax=12,
		]
		\addplot[red,domain=0:5] {10};
		\coordinate (A) at (axis cs:0,10);
	\end{axis}
	\draw (A) node[left] {$ +A $};
\end{tikzpicture}

\begin{tikzpicture}
	\begin{axis}[
			%	title={Alternada},
			axis lines=center,
			ylabel={$ V $},
			xlabel={$ t $},
			xtick=\empty,
			ytick=\empty,
			ymin=-12,ymax=12
		]

		\addplot[domain=0:2*pi,smooth,red] {10*sin(deg(2*x))} coordinate[pos=0.125] (A)
		coordinate[pos=0.375] (B);

		\coordinate (Ao) at (axis cs:0,10);
		\coordinate (Bo) at (axis cs:0,-10);

		\draw[dashed] (Ao) -- (A)
		(Bo) -- (B);
	\end{axis}

	\node[left] at (Ao) {$ +A $};
	\node[left] at (Bo) {$ -A $};

\end{tikzpicture}

%	\begin{tikzpicture}
%	\begin{axis}[
%	axis lines=center,
%	ylabel={$ U(V) $},
%	xlabel={$ t(s) $},
%	xtick=\empty,
%	ytick={0},
%	ymin=-6,ymax=10
%	]
%	
%	\addplot[domain=0:6*pi,smooth,red] {5*sin(deg(x))} coordinate[pos=0.25/3] (A)
%	coordinate[pos=1.25/3] (B)
%	coordinate[pos=0.5/3+1/3] (C)
%	coordinate[pos=0.75/3+2/3] (D)
%	coordinate[pos=0.25/3+2/3] (E)
%	coordinate[pos=0.5/3+2/3] (F);
%	
%	\coordinate (Ao) at (axis cs:0,10);
%	
%	\draw[dashed] (A) -- ++(axis direction cs:0,1) coordinate (Ap) (B) -- ++(axis direction cs:0,1) coordinate (Bp) (B) -- (B-|C) coordinate (Cp) (D) -- (D-|F) coordinate (Fp) (E) -- (E-|F) coordinate (Fpp);
%	
%	\draw[<->] (Ap) -- node[above] {\footnotesize Período $ T $} node[below=-2pt] {\tiny Frequência $ f=\dfrac{1}{T} $} (Bp);
%	
%	\draw[<->] (C) -- node[right] {$ A_m $} (Cp) node[above] {$ V_m $};
%	
%	\draw[<->] (Fp) -- node[very near end,right] {$ U_{pp} $} (Fpp);
%	
%	\end{axis}
%	
%%	\node[left] at (Ao) {$ +A $};
%%	\node[left] at (Bo) {$ -A $};
%	
%	\end{tikzpicture}
%	
%	
%	\begin{tikzpicture}
%	\begin{axis}[
%	axis lines=center,
%	ylabel={$ U(V) $},
%	xlabel={$ t(s) $},
%	xtick=\empty,
%	ytick={0},
%	ymin=-1.5,ymax=3,
%	xmax=3.5*pi
%	]
%	
%	\addplot[domain=0:3*pi,smooth,red] {sin(deg(x))} coordinate[pos=0.25/1.5] (A)
%	coordinate[pos=1/1.5] (B)
%	coordinate[pos=0.5] (C);
%	
%	\coordinate (Ao) at (axis cs:0,10);
%	
%	\draw[dashed] (axis cs:0,1.5) coordinate (Ap) (B) -- (B|-Ap) coordinate (Bp) (axis cs:pi-0.25,0) coordinate (P) (A) -- (A-|P) coordinate (Cp)
%	(C) -- (C-|P) coordinate (Dp);
%	
%	\draw[<->] (axis cs:0,1.5) -- node[above] {\footnotesize Período $ T $} (Bp);
%	
%	\draw[<->] (Cp) node[above] {$ V_m $} -- node[below left] {$ A_m $} (Dp);
%	
%	\end{axis}
%	
%	%	\node[left] at (Ao) {$ +A $};
%	%	\node[left] at (Bo) {$ -A $};
%	
%	\end{tikzpicture}

\end{document}